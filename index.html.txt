<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Will You Be My Valentine?</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: 'Comic Sans MS', 'Arial', sans-serif;
      background: linear-gradient(135deg, #ff9a9e, #fad0c4, #fbc2eb);
      overflow: hidden;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    .page {
      background: rgba(255, 255, 255, 0.95);
      padding: 25px;
      border-radius: 25px;
      text-align: center;
      max-width: 420px;
      width: 90%;
      box-shadow: 0 15px 35px rgba(255, 0, 100, 0.3);
      position: relative;
      z-index: 10;
    }

    h1 {
      color: #e60073;
      margin-bottom: 15px;
    }

    .photo {
      width: 100%;
      border-radius: 20px;
      margin: 15px 0;
      box-shadow: 0 10px 20px rgba(0,0,0,0.2);
    }

    .buttons {
      margin-top: 15px;
      position: relative;
      height: 70px;
    }

    .buttons button {
      padding: 12px 22px;
      font-size: 18px;
      border: none;
      border-radius: 30px;
      margin: 10px;
      cursor: pointer;
      transition: all 0.3s ease;
    }

    #yesBtn {
      background: #ff4d88;
      color: white;
    }

    #noBtn {
      background: #ddd;
      color: #333;
      position: absolute;
      right: 0;
    }

    .hidden {
      display: none;
    }

    /* Celebration Page */
    .celebration h2 {
      color: #e60073;
      margin-top: 10px;
    }

    .celebration img {
      width: 100%;
      border-radius: 20px;
      margin-top: 10px;
    }

    iframe {
      width: 100%;
      height: 200px;
      border-radius: 15px;
      border: none;
      margin-top: 15px;
    }

    /* Floating hearts */
    .heart {
      position: fixed;
      bottom: -20px;
      font-size: 24px;
      animation: floatUp 6s linear infinite;
      opacity: 0.8;
      z-index: 1;
    }

    @keyframes floatUp {
      0% {
        transform: translateY(0) scale(1);
        opacity: 0.9;
      }
      100% {
        transform: translateY(-100vh) scale(1.5);
        opacity: 0;
      }
    }

    /* Fireworks */
    .firework {
      position: fixed;
      width: 6px;
      height: 6px;
      background: transparent;
      border-radius: 50%;
      animation: explode 1s ease-out forwards;
      z-index: 5;
    }

    @keyframes explode {
      0% {
        box-shadow:
          0 0 red,
          0 0 yellow,
          0 0 pink,
          0 0 orange,
          0 0 purple,
          0 0 cyan;
        opacity: 1;
      }
      100% {
        box-shadow:
          -60px -60px red,
          60px -60px yellow,
          -60px 60px pink,
          60px 60px orange,
          0 -80px purple,
          0 80px cyan;
        opacity: 0;
      }
    }
  </style>
</head>
<body>

  <!-- PAGE 1 -->
  <div class="page" id="page1">
    <h1>Shivani Chaurasia, will you be my Valentine? 💖</h1>

    <!-- Your image file should be in same folder -->
    <img src="taj-photo.jpg" class="photo">

    <div class="buttons">
      <button id="yesBtn">Yes 💕</button>
      <button id="noBtn">No 😢</button>
    </div>
  </div>

  <!-- PAGE 2 -->
  <div class="page celebration hidden" id="page2">
    <h2>Yay!!! 💖 I love you, Shivani! 💖💖💖</h2>

    <img src="taj-photo.jpg" class="photo">

    <!-- Celebration GIF -->
    <img src="https://media.giphy.com/media/26BRv0ThflsHCqDrG/giphy.gif">

    <!-- Romantic Hindi Song -->
    <iframe
      src="https://www.youtube.com/embed/vI0T9M_rRXk?autoplay=1&loop=1&playlist=vI0T9M_rRXk"
      allow="autoplay; encrypted-media"
      allowfullscreen>
    </iframe>

    <p style="margin-top: 10px; font-style: italic; color: #b3005e;">— From Kalpesh 💌</p>
  </div>

  <script>
    const yesBtn = document.getElementById("yesBtn");
    const noBtn = document.getElementById("noBtn");
    const page1 = document.getElementById("page1");
    const page2 = document.getElementById("page2");

    let noHoverCount = 0;

    // YES → go to celebration page
    yesBtn.addEventListener("click", () => {
      page1.classList.add("hidden");
      page2.classList.remove("hidden");
      startHearts();
      startFireworks();
    });

    // NO → runs away first 3 times, then allows click
    noBtn.addEventListener("mouseover", () => {
      if (noHoverCount < 3) {
        moveButton();
        noHoverCount++;
      }
    });

    noBtn.addEventListener("click", () => {
      if (noHoverCount >= 3) {
        alert("Oops 😘 You’re supposed to click YES! The correct answer is YES 💖");
      }
    });

    function moveButton() {
      const container = document.getElementById("page1");
      const containerRect = container.getBoundingClientRect();
      const btnRect = noBtn.getBoundingClientRect();

      const maxX = containerRect.width - btnRect.width - 10;
      const maxY = containerRect.height - btnRect.height - 10;

      const randomX = Math.random() * maxX;
      const randomY = Math.random() * maxY;

      noBtn.style.left = randomX + "px";
      noBtn.style.top = randomY + "px";
    }

    // Floating hearts on celebration page
    function startHearts() {
      setInterval(createHeart, 500);
    }

    function createHeart() {
      const heart = document.createElement("div");
      heart.classList.add("heart");
      heart.innerText = "💖";
      heart.style.left = Math.random() * 100 + "vw";
      heart.style.animationDuration = (Math.random() * 3 + 4) + "s";
      heart.style.fontSize = (Math.random() * 20 + 20) + "px";
      document.body.appendChild(heart);

      setTimeout(() => {
        heart.remove();
      }, 6000);
    }

    // Fireworks
    function startFireworks() {
      for (let i = 0; i < 15; i++) {
        setTimeout(createFirework, i * 200);
      }
    }

    function createFirework() {
      const firework = document.createElement("div");
      firework.classList.add("firework");
      firework.style.left = Math.random() * 100 + "vw";
      firework.style.top = Math.random() * 100 + "vh";
      document.body.appendChild(firework);

      setTimeout(() => {
        firework.remove();
      }, 1000);
    }
  </script>

</body>
</html>